<Page x:Class="EF_Core_15.Pages.CategoryPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns:local="clr-namespace:EF_Core_15.Pages"
      xmlns:valid="clr-namespace:EF_Core_15.Validations"
      mc:Ignorable="d"
      Title="CategoryPage">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <ListView 
            ItemsSource="{Binding Categories}"
            SelectedItem="{Binding SelectedCategory}"
            MouseDoubleClick="RefreshChange"
            >
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Name}"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        
        <StackPanel Grid.Column="1">
            <TextBox Name="CategoryName" materialDesign:HintAssist.Hint="Название категории">
                <TextBox.Text>
                    <Binding Path="Newcategoryname" UpdateSourceTrigger="PropertyChanged">
                        <Binding.ValidationRules>
                            <valid:Empty/>
                        </Binding.ValidationRules>
                    </Binding>
                </TextBox.Text>
            </TextBox>
            <ItemsControl ItemsSource="{Binding ElementName=CategoryName,Path=(Validation.Errors)}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding ErrorContent}" Style="{StaticResource ValidationErrorText}"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            <DockPanel LastChildFill="False">
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                    <Button Content="Добавить" IsEnabled="{Binding Addbuttonenabled}" Click="AddCategory"/>
                    <Separator/>
                    <Button Content="Изменить" IsEnabled="{Binding Changebuttonenabled}" Click="ChangeCategory"/>
                    <Separator />
                    <Button Content="Удалить" IsEnabled="{Binding Changebuttonenabled}" Click="DeleteCategory"/>
                </StackPanel>
                <Button Content="Назад" DockPanel.Dock="Right" Click="GoBack"/>
            </DockPanel>
        </StackPanel>
    </Grid>
</Page>
